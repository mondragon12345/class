#include <stdio.h>

int main()
[
float angulo, velocidad,h,x,a,b,c,d,f;
printf ("por favor ingrese el angulo del cañon \n");
scanf ("%f",y angulo);
h=(POW(velocidad,2)*POW(sin(angulo),2))/(2*9.8);
printf("el resultado de la altura es: %\n",h);
x=(POW(velocidad,2)*(sin(2*angulo)))/9.8;
printf("el resultado de la distancia es: %f\n",x);

]
